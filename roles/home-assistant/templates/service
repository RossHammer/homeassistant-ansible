[Unit]
Description=Home Assistant
After=network-online.target

[Service]
Type=simple
User=%i
ExecStart=/srv/homeassistant/bin/hass -c "{{ homeassistant_config_dir }}"

[Install]
WantedBy=multi-user.target
